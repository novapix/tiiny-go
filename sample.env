# Public domain for shortened URLs (replace with your actual domain)
PUBLIC_URL=https://tiiny.example.com

# Default length of generated short keys (8 is typical)
DEFAULT_KEY_LENGTH=8

# Redis connection URL
# Format: redis://<user>:<password>@<host>:<port>/<db>
REDIS_URL=redis://localhost:6379

# Server port
PORT=8080
